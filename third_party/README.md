# Third-party dependencies

Dependencies must be pinned to reviewed revisions. The Godot bridge will use an official
`godot-cpp` revision compatible with Godot 4.7.x. Do not track a floating `main` branch.
