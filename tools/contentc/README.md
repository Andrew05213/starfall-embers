# contentc

Planned TypeScript compiler for project content. It will read `content/src`, validate against JSON
Schema, perform cross-file checks, and write deterministic runtime data into `content/generated`.

No executable is claimed yet; the first implementation arrives with the material schema milestone.
