# 《坠星余烬》长期开发 TODO

> Last revised: 2026-08-03
>
> 本计划描述依赖顺序和验收门槛，不承诺日历日期。当前分支、PR、worktree 和阻塞以
> [`docs/handoff/current.md`](docs/handoff/current.md) 为准。
>
> 最近一次路线图对账：PR #6、PR #5、PR #4、PR #8、PR #9、PR #10 与 PR #11 已合并并完成
> 对应验收；PR #9 已取代并关闭旧 PR #3，P0.3、P2 与 P3 首批“固定向下 SAND↔AIR”已完成。
> 下一项是从最新 main 开始 P4 原生重力分阶段迁移；PR #7 仍由独立工作线处理，不能与本批
> 互相变基或混入。

## 使用规则

- `[x]` 只表示成果已合并到 `main` 且完成对应验收；仅存在于本地分支或 Draft PR 的成果
  仍保持 `[ ]`。
- 默认只推进最早的未完成且未阻塞阶段。允许并行的工作不得改变同一所有权边界、Schema、
  存档格式或权威模拟路径。
- `content/src` 是内容权威源；`content/generated` 只能由 `contentc` 确定性生成。
- 改变模拟所有权、存档兼容性或主要架构边界前必须新增 ADR。
- 性能工作必须记录测试世界尺寸、活跃区块数、命令量和变更前后结果。
- 每项有范围的仓库或 GitHub 操作完成后，同时复核并更新本文件与交接文件。只有成果已合并
  且通过验收时才勾选复选框；没有里程碑变化时记录日期化对账，不虚构进度。分支、PR、CI、
  阻塞和本机路径等易变细节仍只写入交接文件。

## 全局完成标准

每个可合并阶段都必须满足：

- 修改范围单一，生成物、素材与代码来源清楚，没有混入其他 worktree 的本地改动。
- Native 变更完成配置、构建和 CTest；Godot 变更完成 headless import 与
  `res://tests/demo_smoke.gd`。
- 内容变更完成 Schema 校验、确定性重新生成、生成一致性检查和内容测试。
- 性能敏感变更完成对应 benchmark，且没有未解释的退化。
- `git diff --check`、Git LFS 指针检查和适用的 CI 全部通过。
- 用户可见行为、架构边界、冻结决策或操作流程变化已同步文档和交接。

## P0 — 收敛仓库与权威基线

目标：让后续开发只依赖 `main` 上可复现、可拉取且来源明确的基线。

### P0.1 设计资料与 Git LFS

- [x] 推送 `agent/design-baseline-20260726` 及其 LFS 对象。
- [x] 在临时干净克隆中执行 `git lfs pull`，验证 43 PNG、1 ZIP、2 DOCX 均可完整恢复。
- [x] 创建以 `main` 为 base 的设计基线 Draft PR，确认二进制全部为 LFS 指针。
- [x] 完成 CI、审查和来源核对后合并设计基线。
- [x] 保留截断 ZIP 的本机备份；只有权威有效 ZIP 进入仓库。

验收门槛：全新克隆无需本机缓存即可取得全部批准素材，Git 历史不再包含本轮新增的普通大
二进制 blob。

### P0.2 内容台账与首发切片

- [x] 设计基线合并后，将 `agent/content-ledger-well-saga` 变基到最新 `main`。
- [x] 把 PR #5 改为以 `main` 为 base，确认仍只有预期的 16 个内容、编译器、文档和 CI 文件。
- [x] 重新运行 TypeScript、内容生成一致性、内容测试、CTest 和 Godot 合约检查。
- [x] CI 全绿并完成审查后合并 PR #5。
- [ ] 将萨迦概念候选台账和 16 张候选 PNG 放入独立分支；不得混入设计基线或 PR #5。

验收门槛：冻结决策、Schema、运行时 JSON 和可读规格在 `main` 上一致，错误引用和非法状态
由 CI 阻止。

### P0.3 现有 Native PR 收敛

- [x] 先完成 PR #4 的 Gate 1.5 权威切换门槛，再决定 Ready 和合并；不得只因影子模式通过
      就删除 GDScript 回退路径。
- [x] PR #4 合并后，从新 `main` 建立原生区块移植分支。
- [x] 不直接合并当前 PR #3；定向移植其区块存储、批命令、脏区、checksum、测试和 benchmark，
      手工适配 PR #4 的 ballistics 与 `SimulationHost`。
- [x] 移植验证完成后关闭或标记 PR #3 为已被新分支取代，避免两套原生核心分叉。

验收门槛：`main` 上只有一套 C++ 模拟宿主和批处理边界，没有相互覆盖的 PR。

## P1 — Gate 1.5：C++ 弹道成为可切换权威源

目标：完成 Gate 1 已验收战斗手感的原生所有权切换，同时保留安全回退。

- [x] 提供明确的 `gdscript_fallback`、`native_shadow`、`native_authoritative` 三种运行模式。
- [x] 让 C++ 在权威模式中决定弹体推进、寿命、命中、销毁和事件顺序。
- [x] Godot 只批量提交输入并消费快照/事件，不逐弹体、逐像素跨边界调用。
- [x] 确保命中、伤害、退役、音效触发和统计副作用在双跑期间只执行一次。
- [x] 保持固定 30 Hz 权威模拟、显式随机种子和 60 Hz 表现插值。
- [x] 覆盖自由飞行、寿命到期、实体命中、地形命中、追帧和暂停/重启。
- [x] 保持 Gate 1 已锁定射速、弹速、寿命、井星重力和局部 hit-pause 手感。
- [x] Linux、Godot 4.7.1、Windows VS2022 `/MD` CI 全绿。

验收门槛：默认可启用 C++ 权威弹道，影子比较仍可诊断，GDScript 回退可独立运行且结果无
重复副作用。

## P2 — 原生区块、材质存储与批传输

目标：建立后续物质模拟唯一可扩展的数据地基。

- [x] 从 PR #3 定向移植连续单字节 row-major cell 存储和稳定材质编号。
- [x] 固定 64×64 区块，并正确处理右侧、底部非整块边缘。
- [x] 实现预验证的批量绘制、采掘和状态修改命令，非法批次不得留下部分写入。
- [x] 实现稳定 row-major 脏区块批；全部快照构造成功后才能清除 dirty 标记。
- [x] 保留 seed、tick、snapshot、确定性 checksum 和异常安全语义。
- [x] 通过版本化 DTO 连接 Godot；桥接只交换命令批、事件批和脏区块批。
- [x] 建立 1024² 基准并保留更大世界压力测试，报告活跃区块数和批 payload。
- [x] 加入跨语言材质编号协议检查和随机参考模型测试。

验收门槛：C++ 可独立重放同一命令流并产生一致 checksum；Godot 能批量显示脏区块而不读取
逐像素原生状态。

## P3 — M1：原生物质沙盒

目标：把已验证的 GDScript 物质玩法迁移为确定、可扩展的 C++ 权威模拟。

#### 首批固定向下 SAND↔AIR 原生迁移（2026-08-03）

- [x] 在不改材质编号、64×64 区块、DTO v1 或正式混合场景的前提下，将仅 `SAND↔AIR` 的
      固定向下规则迁为 C++ 权威；ROCK、METAL 及其余材质在本批保持静止。
- [x] 完成固定 LCG/扫描顺序、moved 标记、边界与跨区块 dirty 测试，保留独立 GDScript
      参考器逐 tick 对比 cells、random state、dirty batch 和 checksum。
- [x] 完成 1024²、300 tick benchmark，报告 dirty chunks、payload 和 30 Hz 帧预算；PR #11
      已合并，merge commit 为 `afa36c7`。

- [ ] 首批材质编号继续覆盖 AIR、ROCK、SAND、WATER、OIL、FIRE、SMOKE、MOLTEN、
      STEAM、METAL，不在迁移期间随意重排。
- [ ] 分阶段迁移固体、粉末、液体和气体运动，每次只切换一类权威所有权。
- [ ] 接入水灭火、油燃烧、水—熔质反应和温度相变的最小闭环。
- [ ] 接入铁砂导电、一种局部重力影响和一条 3–5 步可中断反应链。
- [ ] 为活跃反应设置每 tick 预算、最大传播深度和稳定的处理顺序。
- [ ] 只激活玩家、高速实体和反应附近区块；远区块休眠或降频。
- [ ] 保留 GDScript 参考模型直到每类迁移完成确定性对比。
- [ ] 更新 benchmark，记录典型与压力场景的世界尺寸、活跃区块和帧预算。

验收门槛：基础物质循环在原生权威模式中可复现、可重放，战斗输入响应不因反应负载明显
退化。

## P4 — M2：可读重力、角色与镜头

目标：让“下方由世界决定”既真实又能被玩家稳定阅读。

> P4 已启动。核心 worktree 为 `C:\tmp\starfall-p4-native-gravity-20260804`，实现提交
> `c98e641` 已通过 Debug CTest 4/4；完整 P4 尚未合并，因此以下复选框保持未勾选。

- [x] 将单坠核径向重力、局部向量场和高速实体重力采样迁入原生核心。
- [ ] 统一角色贴地、跳跃、推进、朝向和镜头的重力查询接口（终审发现正式运行时 Native 局部场链路未推进，暂缓确认）。
- [x] 保持角色物理与镜头表现解耦，镜头不得成为权威重力来源。
- [x] 增加重力向量、坠核影响范围和异常速度调试叠层；区块激活叠层保留到真实 P3 activation batch。
- [ ] 覆盖重力方向快速变化、零重力、双向场边界和高速度穿越（终审确认当前仅有合成零重力测试，待补高速跨场采样）。
- [ ] 用井星已验收移动/镜头用例建立原生与现有行为回归对比（正式 Native provider 链路 blocker 未解除）。

验收门槛：玩家无需依赖文字即可判断局部下方；移动、瞄准和镜头在场切换处不抖动、不翻转
失控。

## P5 — M3：星种战斗与最小执行语言

目标：让星种从普通投射物升级为可构筑、会失稳并能改变环境的微型天体。

- [ ] 完成连射、霰射、蓄力、流束四种射击骨架，验证节奏、瞄准、移动牵制和命中反馈差异。
- [ ] 建立顺序执行、下一项/下一组作用域、延时和碰撞/破裂触发的最小星序语言。
- [ ] 首批组件只覆盖能组成至少五种有效构筑的最小集合，不提前填满全部内容目标。
- [ ] 明确执行预算、嵌套深度、递归限制和确定性错误处理。
- [ ] 接入质量、引力、物质载荷、吸附、反应和生命周期。
- [ ] 实现失稳、泄漏、自伤和死亡原因链，所有结果只由权威模拟产生一次。
- [ ] 用内容 Schema 描述组件、兼容性和数值；运行时不得读取未校验的 authored JSON。
- [ ] 建立空场射击、敌人命中、地形反应和极端构筑压力 benchmark。

验收门槛：至少五种构筑在相同输入/seed 下结果一致，四种射击骨架不依赖换色或倍率冒充差异。

## P6 — 持久化与世界事实

目标：支持“离开后再回来”的世界变化，而不让任务或阵营各自维护冲突真相。

- [ ] 在实现前新增存档 ADR，冻结版本号、迁移策略、失败恢复和兼容窗口。
- [ ] 以 SQLite 保存实体、任务索引和 `WorldFactStore`，大块增量数据采用 zstd 压缩。
- [ ] 保存显式 seed、权威 tick、区块增量、星种状态和因果事件。
- [ ] 任务、阵营文本和环境叙事只读取并解释世界事实，不复制客观状态。
- [ ] 实现原子保存、损坏检测、备份恢复和旧版本迁移测试。
- [ ] 覆盖保存中断、版本升级、缺失内容 ID 和确定性加载后继续模拟。

验收门槛：同一存档加载后继续运行可保持权威状态和关键 checksum；旧存档失败时给出可恢复
路径而不是静默损坏。

## P7 — M4：井星→萨迦首发垂直切片

目标：交付一条从出发、抵达、解决主问题到回访后果的完整可玩链路。

- [ ] 在井星修复旧船并离开；共同下方、坠核和其他总设定路线仍保留。
- [ ] 实现航行过渡、队伍/货物状态和萨迦抵达入口。
- [ ] 实现外昼光钟、内海潮钟、无下城自由陀仪三项校准。
- [ ] 三项全部完成后重建轮坠历，保留萨迦坠核并授予重力与方向见证。
- [ ] 至少推进一次航行后允许回访，并实现中和层校准和第一轮坠可见后果。
- [ ] 五个萨迦区域都呈现轮坠前、中、后状态，但不新增第二个终局选择。
- [ ] 先把概念候选重建为符合 160×90 环境单元、约 20 px 人物、硬边 Alpha 和受控色板的
      生产资产，再接入 Godot。
- [ ] 加入端到端任务状态、存档/加载、失败恢复和回访回归测试。

验收门槛：玩家可从新档完成井星出发、萨迦三校准、一次航行和回访，并在重启游戏后继续；
候选图未经验收不得进入正式场景。

## P8 — Pre-alpha 内容生产与体验完整性

目标：把技术切片提升为可重复试玩、可收集反馈的内部版本。

- [ ] 完成首发切片所需角色朝向、移动、受击、死亡和交互动画。
- [ ] 完成区域碰撞、前中后景分层、灯光、粒子、环境音和交互反馈。
- [ ] 建立敌对生态、资源循环、维修/补给和失败后的可理解恢复路径。
- [ ] 完成键鼠与手柄重映射、字幕、音量分组、震屏/闪烁选项和基础可访问性。
- [ ] 接入本地化键与缺失文本检查，不把显示文本硬编码进模拟核心。
- [ ] 建立首次游玩、重复游玩、极端构筑和低性能机器的试玩清单。
- [ ] 只根据试玩证据调整冻结范围外的数值；冻结决策变更必须更新台账和验收。

验收门槛：不了解设计文档的测试者能完成切片，关键失败原因可读，重复试玩不会因存档或世界
状态漂移而中断。

## P9 — Alpha/Beta 稳定化与首发准备

目标：在扩充完整战役前建立可维护、可发布的产品质量底线。

- [ ] 冻结首发平台和最低配置；Windows 为首发开发平台，Linux CI 保持核心兼容验证。
- [ ] 建立 Release 构建、版本号、资源打包、符号归档和可复现发布流程。
- [ ] 执行长时间 soak、存档迁移、随机输入、fuzz、内存和崩溃恢复测试。
- [ ] 对大世界、最大活跃区块、密集反应和大量实体建立性能预算与降级策略。
- [ ] 完成版权、第三方许可、字体、音频和素材来源审计。
- [ ] 建立回归分级、阻断缺陷标准、候选版本清单和发布后补丁流程。
- [ ] 只有首发切片稳定后，才扩展更多祖星、完整经济、Boss、长线成长和六种终局内容。

验收门槛：候选构建可从干净机器安装、启动、完成切片、保存、升级和卸载；阻断级缺陷为零，
性能与存档兼容性达到已记录门槛。

## 明确延期

在对应前置阶段通过前，不启动以下工作：

- 不在 P5 前批量制作星序组件或复杂武器内容。
- 不在 P6 前实现依赖跨星球回访的长线任务。
- 不在 P7 前扩写完整第二祖星、Boss、经济和完整战役。
- 不让 GPU 承担首版权威物理，也不引入逐像素 Godot/C++ 调用。
- 不规划多人联机；若未来提出，必须先做独立架构与确定性评估。
P4 core CI ledger (2026-08-04): local lifecycle fix `1301ec8` is complete; Debug/Release CTest and the Godot contract check pass. PR #13 remains unchecked pending push and green Godot smoke plus Windows bridge CI.
P4 核心合并对账（2026-08-04）：PR #13 已合并到 `main`（`2f5f0e1`），核心重力与生命周期修复验收完成。P4 的批桥接、shadow、角色/镜头权威切换、诊断和最终 benchmark 仍未完成；下一项从最新 main 开始 `codex/p4-gravity-shadow`。
P4 shadow 对账（2026-08-04）：`39cd1c5` 已完成批量 native gravity source/query DTO、GDExtension bridge、fallback/shadow/authoritative provider 和独立 smoke；本地 CTest 4/4、bridge Debug 编译、Godot 合约检查通过。待推送 Draft PR 后完成 Godot 4.7.1 headless smoke、完整 CI 和 shadow 误差验收；角色/镜头、诊断、benchmark 仍未完成。
P4 角色/镜头对账（2026-08-04）：`eb58c1b` 已完成 `GravityFrame`、零重力滞回、角色统一采样和镜头只读接入，并加入 transition smoke；静态合约检查通过，待 Draft PR CI 验收。诊断叠层与 benchmark 仍未完成。
P4 shadow 合并对账（2026-08-04）：PR #15 已合并到 `main`（`98177eb`），批桥接与 shadow smoke 验收完成。下一项为 `codex/p4-player-camera-gravity` 的 GravityFrame/角色/镜头权威切换；诊断叠层和 benchmark 仍未完成。
P4 角色/镜头合并对账（2026-08-04）：PR #17 已合并到 `main`（`606614f`），最终 head `f52e42e` 的 push 与 pull_request 两组 CI 均 8/8 通过。GravityFrame、零重力滞回、角色统一采样和镜头只读接入验收完成；诊断叠层、1024² 重力/粉末 benchmark、真实区块激活调度和最终 GPT-5.6-sol xhigh 只读复审仍未完成。
P4 diagnostics 对账（2026-08-04）：`codex/p4-gravity-diagnostics` 从 `origin/main=d0bc415` 开始，实现提交 `268b15c`，Draft PR #19 已创建。已加入可选重力诊断叠层、native gravity bridge smoke、32 局部场/4096 查询/300 tick 的 1024² benchmark、批量确定性 CTest、高速弹体子步与采样上限指标，并明确 dirty chunks 不等于 activation scheduling。Python Godot 合约检查通过；本机因缺少 C++ 编译器无法运行 CMake/CTest，等待远端 CI。PR #19 未合并前保持 P4 diagnostics 与 benchmark 未勾选；真实区块激活调度和最终 GPT-5.6-sol xhigh 只读复审仍未完成。
P4 diagnostics 合并对账（2026-08-04）：PR #19 已合并到 `main`（`176d7c7`），最终 head `ee9d189` 的 push 与 pull_request 两组 CI 均 8/8 通过。诊断叠层、bridge smoke、1024²/32 场/4096 查询/300 tick 重力 benchmark、高速弹体指标和 P3 dirty-chunk 统计已验收；真实区块激活调度仍未完成，最终 GPT-5.6-sol xhigh 只读复审待 P4 全部实现后执行。
P4 final review 对账（2026-08-04）：GPT-5.6-sol xhigh 只读终审结论为 BLOCKED；不修改代码。P0 blocker：`game/scripts/demo/player_controller.gd` 创建的私有 Native host 在正式运行时没有 `step_fixed()`，而 `main.gd`/`starseed.gd` 仍只向 MaterialWorld 写局部场，导致 GDExtension 正式运行不推进 Native tick/TTL，局部重力行为回退。P1 blocker：角色只采当前位置，未将当前位置与预测位置放入同一批，`GravityFrame.transitioning` 只覆盖零重力滞回；高速跨场/双向边界验收不成立。P1 blocker：`native_gravity_provider.gd` 的径向 source ID 与 `add_uniform_source()` 的 request counter 不共享命名空间，可能重复 ID；常向量场也未写入 GDScript shadow。非阻断风险：诊断叠层无正式消费者、bridge 异常码细分不足、弹体 benchmark 仍为 1200 px/s（子步 2、上限命中 0）、current 顶部历史状态仍需后续清理。所有 blocker 保留，后续修复需另行授权。
最终落地对账（2026-08-04）：上述 blocker 文档已随 PR #21 进入 `main`，merge commit 为 `cc42889`；终审后未修改实现。后续只允许在新授权下处理 blocker，P4 不得标记为无条件完成。
CI 基础设施对账（2026-08-04）：最终 `main` merge commit `cf4f6ee` 的 push workflow `30853826868` 两次均在无 job step/log 的 runner 启动阶段失败；PR #22 两组 CI 与此前 main workflow 通过。保留为外部 blocker，待 Actions 恢复后重新核对，不绕过保护检查、不修改实现。
