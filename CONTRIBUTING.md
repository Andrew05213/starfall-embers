# 参与开发

项目仍处于架构和原型阶段。提交应保持小而可验证，并说明它验证了哪条玩法或技术假设。

## 分支与提交

- 从短生命周期功能分支开发，避免把无关改动混进同一提交。
- 提交信息使用祈使句，并标注主要范围，例如 `sim: add fixed-step world clock`。
- 不提交构建产物、本地缓存、密钥、未授权设计文档或来源不明的素材。

## 完成标准

- 行为改变有对应测试或明确的人工验证步骤。
- 新增性能热点有基准或 profiler 证据。
- 新内容通过 Schema、引用完整性和物理—文本一致性校验。
- 用户可见术语遵守文案手册：系统名稳定、可检索，诗意表达不遮蔽操作信息。

## 架构改动

以下变更必须在 `docs/adr/` 新增记录：模块所有权、Godot/C++ 边界、存档格式、内容生成规则、确定性策略和第三方核心依赖。
